import React from 'react';
import Header from './Header';

const PageNotFound = () => {
    return (
        <div>
            <Header/>
            <h1>PageNotFound</h1>
        </div>
    );
};

export default PageNotFound;